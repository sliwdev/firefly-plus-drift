# Firefly+  
**Sub-10ns sync every 5s instead of 1s — using drift prediction.**  

> **VIBE-CODED IN ~10 MINUTES**  
> **NOT TESTED. MIGHT WORK. MIGHT SUMMON CLOCK DEMONS.**  
> If it works — cool. If not — expected.  
> *Built in ~10 mins (including a break). No shame.*

---

**Why?**  
Firefly syncs every 1s (by default) → ~6–8 ns error.  
But drift is predictable.  
→ Sync every **5s** + light probes → same accuracy, 5× less traffic.

---

**PoC only.** Pull requests welcome.
